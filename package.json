{
  "name": "migraine-log-fullstack",
  "version": "1.0.0",
  "private": true,
"scripts": {
  "heroku-postbuild": "cd migraine-log && npm install && npm run build",
  "start:prod": "cd migraine-log-api && npm start"
},
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": "22.x"
  }
  
}
